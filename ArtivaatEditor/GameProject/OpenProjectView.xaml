<UserControl x:Class="ArtivaatEditor.GameProject.OpenProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <ListBox Width="195"
                     Height="250"/>

            <Border Width="400"
                    Height="250"
                    Margin="10,0,0,0"
                    BorderBrush="Gray"
                    BorderThickness="1"/>

        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20">

            <Button Width="100"
        Height="30"
        Content="Create"
        IsDefault="True"/>

            <Button Width="100"
        Height="30"
        Margin="30,0,0,0"
        Content="Exit"
        IsCancel="True"/>

        </StackPanel>

    </Grid>

</UserControl>